import { ChevronRight, Printer } from 'lucide-react'

export default function MasterRecordDetail({eyebrow,id,title,status,statusTone='green',onBack,groups=[]}){
  const summary=groups.flatMap(group=>group.items).slice(0,4)
  return <section className="master-detail-page"><header className="record-page-header"><div className="record-header-copy"><div className="record-header-nav"><button className="back-link" onClick={onBack}>← Back to register</button><span className="record-kicker">{eyebrow}</span></div><div className="wo-title-line"><h1>{id}</h1><span className={`badge ${statusTone}`}><i/>{status}</span></div><p>{title}</p></div><div className="record-header-actions"><button className="outline" onClick={()=>window.print()}><Printer size={15}/>Print record</button></div></header><section className="master-record-summary">{summary.map(([label,value])=><div key={label}><span>{label}</span><strong>{value||'—'}</strong></div>)}</section><nav className="record-tabs"><button className="active">Record Details</button></nav><main className="master-detail-content">{groups.map(group=><section className="master-detail-card" key={group.title}><header><div><span>{group.kicker}</span><h2>{group.title}</h2></div><ChevronRight size={17}/></header><dl>{group.items.map(([label,value])=><div key={label}><dt>{label}</dt><dd>{value||'—'}</dd></div>)}</dl></section>)}</main></section>
}
